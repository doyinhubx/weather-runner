apiVersion: v1
kind: Service
metadata:
  name: weather-app
  namespace: app
spec:
  selector:
    app: weather-app
    version: blue   # Default: start with Blue
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP

apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-blue
  namespace: app
  labels:
    app: weather-app
    version: blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: weather-app
      version: blue
  template:
    metadata:
      labels:
        app: weather-app
        version: blue
    spec:
      containers:
        - name: weather-app
          image: weather-app:local
          ports:
            - containerPort: 5000

title = "Security Scan Configuration"

# Load default rules PLUS your custom rules
load-default = true

[[rules]]
id = "generic-api-key"
description = "Generic API Key"
regex = '''(?i)(?:key|api|token|secret)[\s:=]+([a-z0-9\-_]{20,})'''
tags = ["key", "api-key"]
entropy = 3.5
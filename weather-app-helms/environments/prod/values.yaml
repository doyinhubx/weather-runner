# weather-app-helms/environments/prod/values.yaml
image:
  repository: eslintpurity/weather-app
  tag: "38f0b39-helms-prod"  # This gets overwritten automatically
  pullPolicy: Always
service:
  type: ClusterIP
  port: 80
  targetPort: 8080
ingress:
  enabled: true
  hostname: prod.helm.weather.local